require_relative 'config/environment'
# require 'sinatra/activerecord/rake'

desc "Create seed data for users, rides, and drivers"
task :seed do 
  ruby "db/seeds.rb"
end

desc "This will start a binding.pry"
task :console do 
  binding.pry
end

